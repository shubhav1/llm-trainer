[project]
name = "transformer-benchmark"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "einops",
    "einx",
    "jaxtyping",
    "numpy",
    "torch",
]

[tool.uv]
package = false
